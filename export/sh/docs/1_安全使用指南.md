# Docker Export Compose - 安全使用指南 / Security Guide

## 📋 文档信息 / Document Information

- **版本 / Version**: v2.2
- **创建日期 / Created**: 2025-11-06
- **最后更新 / Last Update**: 2025-11-06
- **适用于 / For**: docker-export-compose.sh v2.2+

---

## 🔒 安全性增强概述 / Security Enhancements Overview

v2.2 版本引入了多项重要的安全性改进：

### ✅ 新增安全特性 / New Security Features

1. **隐私模式 (`--privacy`)** - 隐藏主机路径
2. **核心目录保护** - 防止意外写入系统目录
3. **容器名验证** - 防止路径遍历攻击
4. **敏感信息检测** - 50+ 关键词模式识别
5. **UTF-8 编码** - 避免输出乱码
6. **安全的默认目录** - `./output/` 而非当前目录
7. **环境变量引用** - env 模式使用 `${VAR}` 引用
8. **警告系统** - yml 模式明文敏感信息警告

---

## 🚨 重要安全警告 / Critical Security Warnings

### ⚠️ yml 模式风险 / yml Mode Risks

**默认的 yml 模式会将所有环境变量以明文形式写入 docker-compose.yml**

```yaml
# ⚠️ 危险示例 / DANGEROUS Example
environment:
  - MYSQL_ROOT_PASSWORD=super_secret_123  # ⚠️ 明文密码！
  - API_KEY=sk-1234567890abcdef           # ⚠️ 明文密钥！
```

**风险 / Risks:**
- ✗ 提交到 Git 仓库
- ✗ 分享给他人
- ✗ 存储在不安全的位置
- ✗ 被日志系统记录

**推荐做法 / Recommended:**

```bash
# 使用 env 模式（安全）
./docker-export-compose.sh --type env my-container
```

### ⚠️ 数据卷路径暴露 / Volume Path Exposure

**默认模式会暴露主机完整路径：**

```yaml
volumes:
  - /home/username/projects/sensitive-app/data:/app/data
```

**风险 / Risks:**
- 泄露用户名
- 泄露项目结构
- 泄露敏感目录位置

**推荐做法 / Recommended:**

```bash
# 使用隐私模式
./docker-export-compose.sh --privacy my-container
```

输出结果：
```yaml
volumes:
  - /path/to/data:/app/data  # 原路径已隐藏 / Original: /home/username/...
```

---

## 📖 安全最佳实践 / Security Best Practices

### 1. 选择正确的导出模式

#### 开发环境 / Development

```bash
# yml 模式（仅限本地开发）
./docker-export-compose.sh my-container

# ⚠️ 注意：不要提交到 Git！
```

#### 生产环境 / Production

```bash
# env 模式 + 隐私模式（推荐）
./docker-export-compose.sh --type env --privacy my-container
```

#### 分享配置 / Sharing Configurations

```bash
# 使用 env 模式生成模板
./docker-export-compose.sh --type env --privacy my-container

# 只分享这些文件：
# ✓ docker-compose.yml
# ✓ .env.example
# ✓ README.md
# ✗ .env (不要分享！)
```

### 2. 输出目录安全

#### ✅ 安全的输出目录

```bash
# 默认输出（推荐）
./docker-export-compose.sh my-container
# 输出到：./output/my-container/

# 用户主目录
./docker-export-compose.sh -o ~/docker-exports my-container

# 临时目录
./docker-export-compose.sh -o /tmp/docker-exports my-container
```

#### ❌ 危险的输出目录

```bash
# 系统核心目录（会被拦截）
./docker-export-compose.sh -o /etc my-container
# ⚠️ 错误：禁止输出到核心系统目录！

# 如果确实需要（非常危险！）
./docker-export-compose.sh -o /etc --must-output my-container
# 需要三次确认：YES → I-AM-SURE → FORCE-OUTPUT
```

### 3. 容器名安全验证

#### ✅ 安全的容器名

```bash
./docker-export-compose.sh nginx-web
./docker-export-compose.sh my-app-v2
./docker-export-compose.sh db_mysql_prod
```

#### ❌ 危险的容器名（会被拒绝）

```bash
# 路径遍历攻击
./docker-export-compose.sh "../../../etc/malicious"
# ⚠️ 错误：容器名包含非法字符（路径遍历攻击）

# 包含危险字符
./docker-export-compose.sh "my-app; rm -rf /"
# ⚠️ 错误：容器名包含危险字符
```

### 4. 敏感信息处理

#### 扩展的敏感关键词列表

脚本会自动检测以下类型的敏感环境变量：

**密码相关 / Passwords:**
- PASSWORD, PASSWD, PWD, PASS

**密钥和令牌 / Keys & Tokens:**
- SECRET, TOKEN, KEY, APIKEY
- API_KEY, API_SECRET, API_TOKEN
- ACCESS_KEY, ACCESS_TOKEN, ACCESS_SECRET
- PRIVATE_KEY, PUBLIC_KEY, SSH_KEY

**认证相关 / Authentication:**
- AUTH, AUTHENTICATION, AUTHORIZATION
- CREDENTIALS, CREDENTIAL
- SESSION, SESSION_KEY, SESSION_SECRET
- OAUTH, OAUTH_TOKEN, OAUTH_SECRET

**证书相关 / Certificates:**
- CERT, CERTIFICATE, SSL, TLS
- PRIVATE, PEM, P12, PKCS

**加密相关 / Encryption:**
- SALT, HASH, ENCRYPTION, DECRYPT
- CIPHER, AES, RSA

**数据库连接 / Database:**
- DATABASE_URL, DB_PASSWORD, DB_USER
- CONNECTION_STRING, CONN_STR
- MYSQL_PASSWORD, POSTGRES_PASSWORD
- MONGO_PASSWORD, REDIS_PASSWORD

**管理员相关 / Admin:**
- ADMIN, ROOT, SUPERUSER
- ADMIN_PASSWORD, ROOT_PASSWORD

**云服务密钥 / Cloud Keys:**
- AWS_SECRET, AWS_ACCESS, AWS_KEY
- AZURE_, GCP_, GOOGLE_
- CLOUD_, S3_

**其他 / Others:**
- SIGNING_KEY, JWT_SECRET, JWT_KEY
- WEBHOOK_SECRET, ENCRYPTION_KEY

#### 自定义敏感关键词 (v2.2 新增) ⭐

除了以上 50+ 个内置关键词，您还可以通过 `config` 文件添加**组织或项目特定**的敏感关键词。

**创建 config 文件**:
```bash
# 进入脚本目录
cd export/sh

# 编辑 config 文件（首次运行会自动创建模板）
nano config
```

**添加自定义关键词**:
```bash
# config 文件示例
# 公司特定命名
MYCOMPANY_SECRET
COMPANY_API_KEY
INTERNAL_TOKEN

# 项目特定命名
PROJECT_AUTH_KEY
CUSTOM_ENCRYPTION_KEY
```

**详细说明**: 请参阅 [CONFIG_FILE_GUIDE.md](CONFIG_FILE_GUIDE.md)

---

## 🔧 使用场景示例 / Usage Scenarios

### 场景 1：本地开发测试

```bash
# 快速导出查看配置
./docker-export-compose.sh my-dev-app

# 查看生成的文件
cat output/my-dev-app/docker-compose.yml

# 注意：包含明文敏感信息，仅限本地使用！
```

### 场景 2：准备生产环境部署

```bash
# 使用 env 模式 + 隐私模式
./docker-export-compose.sh --type env --privacy my-prod-app

# 检查生成的文件
cd output/my-prod-app
ls -la

# 输出：
# docker-compose.yml  - 使用 ${VAR} 引用
# .env                - 实际的敏感数据（已加入 .gitignore）
# .env.example        - 安全的模板
# .gitignore          - 自动生成
# README.md           - 使用说明

# 提交到 Git（.env 会被自动忽略）
git add .
git commit -m "Add production config"
```

### 场景 3：批量导出所有容器

```bash
# 导出所有运行中的容器（env 模式）
./docker-export-compose.sh --all-run --type env -o ~/backup

# 查看结果
tree ~/backup/

# ~/backup/
# ├── nginx-web/
# │   ├── docker-compose.yml
# │   ├── .env
# │   └── .env.example
# ├── mysql-db/
# │   ├── docker-compose.yml
# │   ├── .env
# │   └── .env.example
# └── ...
```

### 场景 4：分享配置给团队

```bash
# 1. 使用 env + privacy 模式导出
./docker-export-compose.sh --type env --privacy my-app

# 2. 进入目录
cd output/my-app

# 3. 只分享这些文件
git add docker-compose.yml .env.example README.md .gitignore
git commit -m "Add app configuration"
git push

# 4. 团队成员使用
# git clone <repo>
# cp .env.example .env
# nano .env  # 填写实际值
# docker compose up -d
```

### 场景 5：强制输出到特殊目录（高级）

```bash
# 尝试输出到系统目录
./docker-export-compose.sh -o /etc my-container

# 输出：
# ⚠️ 严重警告 / CRITICAL WARNING ⚠️
# 您正在尝试输出到系统核心目录！
# 目标目录 / Target: /etc
# 
# 这可能会：
#   ✗ 破坏系统文件 / Damage system files
#   ✗ 导致安全风险 / Cause security risks
#   ✗ 使系统无法启动 / Make system unbootable
# 
# 第一次确认: 您确定要继续吗？(输入 YES 继续): YES
# 第二次确认: 您真的确定吗？(输入 I-AM-SURE 继续): I-AM-SURE
# 第三次确认: 最后一次机会！(输入 FORCE-OUTPUT 继续): FORCE-OUTPUT

# 或者直接使用 --must-output 跳过确认（非常危险！）
./docker-export-compose.sh -o /etc --must-output my-container
```

---

## 🛡️ 安全检查清单 / Security Checklist

### 导出前 / Before Export

- [ ] 确定导出目的（开发/生产/分享）
- [ ] 选择合适的模式（yml/env）
- [ ] 检查是否需要隐私模式
- [ ] 确认输出目录安全

### 导出后 / After Export

- [ ] 检查生成的文件内容
- [ ] 验证敏感信息是否被正确处理
- [ ] 确认 .env 文件已被 .gitignore
- [ ] 测试配置是否可用

### 分享前 / Before Sharing

- [ ] 确认不包含 .env 文件
- [ ] 检查 docker-compose.yml 是否有明文敏感信息
- [ ] 验证 .env.example 中敏感值已被替换
- [ ] 确认路径信息已被隐藏（如使用了 --privacy）

### 生产部署前 / Before Production

- [ ] 使用 env 模式
- [ ] 启用隐私模式
- [ ] 验证所有敏感信息已从配置文件中移除
- [ ] 测试 .env 文件加载正常
- [ ] 备份原始 .env 文件

---

## 🔍 故障排查 / Troubleshooting

### 问题 1：输出被拦截

```bash
# 错误
[ERROR] 禁止输出到核心系统目录！

# 原因
输出目录是系统核心目录（/etc, /bin 等）

# 解决方案
# 1. 使用安全目录（推荐）
./docker-export-compose.sh -o ~/exports my-container

# 2. 使用默认目录
./docker-export-compose.sh my-container

# 3. 强制输出（不推荐）
./docker-export-compose.sh -o /etc --must-output my-container
```

### 问题 2：容器名被拒绝

```bash
# 错误
[ERROR] 容器名包含非法字符（路径遍历攻击）

# 原因
容器名包含 ../ 或其他危险字符

# 解决方案
# 使用正确的容器名
docker ps  # 查看实际容器名
./docker-export-compose.sh <正确的容器名>
```

### 问题 3：中文乱码

```bash
# 现象
生成的文件中中文显示为乱码

# 原因
终端编码不是 UTF-8

# 解决方案
# v2.2 已自动设置 UTF-8 编码
# 如仍有问题，手动设置：
export LANG=C.UTF-8
export LC_ALL=C.UTF-8
./docker-export-compose.sh my-container
```

### 问题 4：.env 文件无法加载

```bash
# 现象
docker compose up 时环境变量为空

# 原因
.env 文件格式错误或位置不对

# 解决方案
# 1. 检查 .env 文件位置
ls -la .env  # 应该在 docker-compose.yml 同级目录

# 2. 检查 .env 文件格式
cat .env
# 格式应该是：VAR_NAME=value

# 3. 验证配置
docker compose config
```

---

## 📚 参考文档 / References

### 相关文档

- [README.md](README.md) - 脚本基础使用说明
- [融合脚本使用指南.md](Docker%20Export%20Compose%20融合脚本使用指南.md) - 详细使用教程

### 命令速查

```bash
# 基础命令
./docker-export-compose.sh my-container                    # 默认模式
./docker-export-compose.sh --type env my-container         # env 模式
./docker-export-compose.sh --privacy my-container          # 隐私模式
./docker-export-compose.sh --type env --privacy my-app     # 组合使用（推荐）

# 批量操作
./docker-export-compose.sh --all-run                       # 所有运行中
./docker-export-compose.sh --file containers.txt           # 从文件
./docker-export-compose.sh --all --type env -o ~/backup    # 组合

# 帮助信息
./docker-export-compose.sh --help                          # 英文帮助
./docker-export-compose.sh --help-cn                       # 中文帮助
./docker-export-compose.sh --version                       # 版本信息
```

---

## ⚡ 快速决策树 / Quick Decision Tree

```
需要导出容器配置？
│
├─ 用于本地开发测试？
│  └─ ./docker-export-compose.sh my-container
│
├─ 用于生产环境部署？
│  └─ ./docker-export-compose.sh --type env --privacy my-container
│
├─ 需要分享给团队？
│  └─ ./docker-export-compose.sh --type env --privacy my-container
│     然后只分享 docker-compose.yml 和 .env.example
│
├─ 批量备份所有容器？
│  └─ ./docker-export-compose.sh --all-run --type env -o ~/backup
│
└─ 快速查看配置？
   └─ ./docker-export-compose.sh --dry-run my-container
```

---

## 🎯 总结 / Summary

### 核心安全原则

1. **最小权限原则** - 使用安全的输出目录
2. **敏感信息分离** - 生产环境使用 env 模式
3. **隐私保护** - 分享时使用 privacy 模式
4. **验证优先** - 导出前验证容器名安全
5. **警告注意** - 重视脚本的安全警告

### 推荐配置

**开发环境：**
```bash
./docker-export-compose.sh my-app
```

**生产环境：**
```bash
./docker-export-compose.sh --type env --privacy my-app
```

**分享/备份：**
```bash
./docker-export-compose.sh --type env --privacy --all-run -o ~/backup
```

---

**文档版本**: v2.2  
**最后更新**: 2025-11-06  
**维护者**: clearlove.ymg  
**许可证**: MIT License

